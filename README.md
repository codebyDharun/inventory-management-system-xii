# 📦 Inventory Management System

The **Inventory Management System** is a comprehensive command-line application developed in **Python** with **MySQL** as its database backend.  
It is designed to provide seamless management of product inventories, including functionalities such as product addition, updates, sales tracking, and real-time stock monitoring.

---

## 🧠 Overview

This system simplifies the process of maintaining product data within a database, allowing users to efficiently record transactions, monitor stock levels, and ensure data integrity.  
It offers a straightforward interface and robust functionality suitable for small to medium-scale retail or warehouse operations.

---

## 🚀 Key Features

- **Product Management:** Add, edit, search, and delete product records.  
- **Sales Processing:** Record product sales and automatically update available stock.  
- **Price and Quantity Tracking:** Retrieve and modify product pricing and quantity information.  
- **Automated Billing:** Generate clean, formatted bills for every sale.  
- **Database Integration:** Persistent data storage and management using MySQL.  
- **Error Handling:** Built-in validation for user input and database operations.  
- **User-Friendly Interface:** Simple, text-based navigation menu for streamlined interaction.

---

## ⚙️ Technologies Used

| Component | Description |
|------------|-------------|
| **Language** | Python 3.x |
| **Database** | MySQL 8.0 |
| **Connector** | mysql-connector-python |
| **Interface** | Command-Line (CLI) |

---

## 🧩 Core Functionalities

- `show_all()` — Displays all product records.  
- `add_entry()` — Adds a new product to the inventory.  
- `update_qty()` — Updates the stock quantity for a specific item.  
- `change_price()` — Adjusts the price of an existing product.  
- `Sales()` — Records a sale, updates stock, and generates a sales receipt.  
- `delete_entry()` — Deletes a product from the database.  
- `header()` — Displays the main menu interface.

---
